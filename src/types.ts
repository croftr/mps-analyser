export interface Vote {
    DivisionId: number,
    Title: string,
    Date: string
  }

  export interface Party {
    name: string,
    total: number,
    backgroundColour: string
    foregroundColour: string,
  }